# Copy to .env and fill in values as needed.
# cp .env.example .env

# Production-like behavior when set to prod or production
# REACT_APP_ENV=prod

# Sanity CMS (optional — token descriptions; skip if unset)
# REACT_APP_SANITY_PROJECT_ID=
# REACT_APP_SANITY_DATASET=mainnet
# REACT_APP_SANITY_AUTH_TOKEN=
